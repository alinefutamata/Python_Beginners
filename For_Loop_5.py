# Write a function remove_duplicates that takes in a list
# and removes elements of the list that are the same.
# For example: remove_duplicates([1,1,2,2]) should return [1,2].

list = [1, 2, 3, 5, 4, 6, 3, 4, 5, 1, 1, 2]

for item in list:
    if item == list
        item.